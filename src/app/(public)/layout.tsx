// Keep public information outside (common)/loading.tsx so the homepage and
// legal text are visible in the initial HTML without JavaScript.
export { default } from "@/components/layout/SiteLayout";
